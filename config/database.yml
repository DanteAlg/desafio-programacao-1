default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  user: postgres

development:
  <<: *default
  database: nexxas_development

test:
  <<: *default
  database: nexxas_test
